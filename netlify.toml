# Used when Netlify's base directory is already set to `gamers-unite`.
# Do not set `publish` — the Netlify Next.js runtime owns the output.
[build]
  command = "pnpm build"

[build.environment]
  NODE_VERSION = "20"
  NEXT_PUBLIC_API_BASE_URL = "/api/v1"
  NEXT_PUBLIC_APP_ENV = "production"
